
const path = require('path');
const fs = require("fs");

// exports.privateKey = fs.readFileSync(path.resolve('/privkey.pem')).toString();
// xports.certificate = fs.readFileSync(path.join('/fullchain.pem')).toString();